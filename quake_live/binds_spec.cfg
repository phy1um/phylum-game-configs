unbindall


bind mouse2 "+moveup"
bind mouse1 "+attack"
bind mouse3 "+button2"
bind space "+attack"

bind F1 "vote yes"
bind F2 "vote no"
bind F3 "readyup"

bind TAB "+scores"
bind h "+chat"

bind a "+acc"

bind z "spec pu"
bind x "spec red"
bind c "spec blue"

bind 1 "follow 1"
bind 2 "follow 2"
bind 3 "follow 3"
bind 4 "follow 4"
bind 5 "follow 5"
bind 6 "follow 6"
bind 7 "follow 7"
bind 8 "follow 8"
bind 9 "follow 9"
bind 0 "follow 0"

exec binds_util

