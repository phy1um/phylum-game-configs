// DUEL BINDS

unbindall

bind e "+forward"
bind d "+back"
bind s "+moveleft"
bind f "+moveright"

bind p "messagemode"
bind y "messagemode2"

bind mouse2 "+moveup"
bind mouse1 "+attack"
bind mouse3 "+button2"

bind a "+zoom"

bind CTRL "+movedown"
bind SHIFT "+speed"

bind F1 "vote yes"
bind F2 "vote no"
bind F3 "readyup"

bind TAB "+scores"
bind h "+chat"

bind 2 "say ^3:]^7"

//This doesn't work outside of practice?
//seta _sm1 "say ^6 :]; bind 2 vstr _sm2"
//seta _sm2 "say ^8 :]; bind 2 vstr _sm1"

exec binds_weapons.cfg
exec binds_util.cfg

